<h3><%= @category.name %></h3>

<% @category.articles.each do |article| %>
  <p class="lead"><%= link_to article.title, articles_path(@category.name, article.id) %> -- <%= time_ago_in_words(article.created_at) %> ago</p>
<% end %>
